<h2>商品一覧</h2><h4>全件</h4>

<% @items.each do |item| %>
<div>
  <%= link_to item_path(item) do %>
    <%= attachment_image_tag item, :image, :fill, 150, 100, format:'jpeg' %>
  <% end %>
  <%= item.name %>
  <%= item.price %>
</div>

<% end %>